# Prefix then A opens a persistent multi-turn cockpit for the originating pane.
bind-key A display-popup -EE -d "#{pane_current_path}" -w 92% -h 85% -T " SecurityLlama cockpit " -- @SECURITYLLAMA_EXECUTABLE@ cockpit --pane "#{pane_id}"
